<UserControl x:Class="KarzounERP.Views.Companies.CompanyListPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             FlowDirection="{DynamicResource AppFlowDirection}"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <materialDesign:PackIcon Kind="OfficeBuilding" Width="28" Height="28" Foreground="{DynamicResource AppPrimaryBrush}" VerticalAlignment="Center" />
            <TextBlock Text="{DynamicResource Comp_Title}" FontSize="24" FontWeight="Bold" FontFamily="{DynamicResource AppHeadlineFontFamily}" Foreground="{DynamicResource BrandTextPrimary}" Margin="8,0,0,0" VerticalAlignment="Center" />
            <Button Content="{DynamicResource Comp_New}"
                    Command="{Binding AddCompanyCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{DynamicResource AppButtonBrush}" BorderBrush="{DynamicResource AppButtonBrush}"
                    Margin="20,0,0,0" VerticalAlignment="Center" />
        </StackPanel>

        <ProgressBar Grid.Row="1" IsIndeterminate="True" Height="3" Margin="0,0,0,8"
                     Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}" />

        <!-- Companies DataGrid -->
        <materialDesign:Card Grid.Row="2" Padding="0" UniformCornerRadius="8">
            <DataGrid ItemsSource="{Binding Companies}"
                      SelectedItem="{Binding SelectedCompany}"
                      AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      materialDesign:DataGridAssist.CellPadding="12 8"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="44">
                        <DataGridTemplateColumn.HeaderTemplate>
                            <DataTemplate>
                                <CheckBox IsThreeState="True"
                                          IsChecked="{Binding DataContext.AreAllCompaniesSelected, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.HeaderTemplate>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          Command="{Binding DataContext.SelectionChangedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="{DynamicResource Comp_ColName}" Binding="{Binding Name}" Width="2*" MinWidth="170" />
                    <DataGridTextColumn Header="{DynamicResource Comp_ColCommercial}" Binding="{Binding CommercialName}" Width="2*" MinWidth="170" />
                    <DataGridTextColumn Header="{DynamicResource Comp_ColPhone}" Binding="{Binding Phone}" Width="140" MinWidth="120" />
                    <DataGridTextColumn Header="{DynamicResource Comp_ColCurrency}" Binding="{Binding Currency}" Width="90" MinWidth="80" />
                    <DataGridTextColumn Header="{DynamicResource Comp_ColTax}" Binding="{Binding TaxRate, StringFormat=N1}" Width="90" MinWidth="80" />
                    <DataGridTemplateColumn Header="{DynamicResource Comp_ColActions}" Width="230" MinWidth="210">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="{DynamicResource Btn_Activate}"
                                            Command="{Binding DataContext.SetActiveCompanyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource MaterialDesignFlatButton}"
                                            Foreground="{DynamicResource BrandSuccess}" Padding="8,4" />
                                    <Button Content="{DynamicResource Btn_Edit}"
                                            Command="{Binding DataContext.EditCompanyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource MaterialDesignFlatButton}"
                                            Foreground="{DynamicResource BrandInfo}" Padding="8,4" />
                                    <Button Content="{DynamicResource Btn_Delete}"
                                            Command="{Binding DataContext.DeleteCompanyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource MaterialDesignFlatButton}"
                                            Foreground="{DynamicResource BrandDanger}" Padding="8,4" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>

        <DockPanel Grid.Row="3" Margin="0,8,0,0">
            <TextBlock Foreground="#888" VerticalAlignment="Center">
                <Run Text="{DynamicResource Bulk_Selected}" />
                <Run Text="{Binding SelectedCount}" FontWeight="Bold" />
            </TextBlock>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Visibility="{Binding SelectedCount, Converter={StaticResource BoolToVisibility}}">
                <Button Content="{DynamicResource Btn_Clear}" Command="{Binding ClearSelectionCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" />
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>
